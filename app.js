const canvas=new fabric.Canvas('canvas',{preserveObjectStacking:true,selection:true});const upload=document.getElementById('upload');const profileName=document.getElementById('profileName');const slotButtons=[...document.querySelectorAll('.slot-btn')];const statusBox=document.getElementById('status');const resultsBody=document.querySelector('#resultsTable tbody');let profiles=JSON.parse(localStorage.getItem('ocrLayoutProfiles')||'null');if(!Array.isArray(profiles)||profiles.length!==4)profiles=[null,null,null,null];let activeSlot=0;let originalImage=null;let imageScale=1;let currentImageDataUrl='';let results=[];function setStatus(msg){statusBox.textContent=msg;}function persistProfiles(){localStorage.setItem('ocrLayoutProfiles',JSON.stringify(profiles));}function refreshSlotButtons(){slotButtons.forEach((btn,i)=>{btn.classList.toggle('active',i===activeSlot);btn.textContent=(profiles[i]&&profiles[i].name?profiles[i].name:'Bolsillo '+(i+1));});profileName.value=profiles[activeSlot]&&profiles[activeSlot].name?profiles[activeSlot].name:'';}function makeRect(kind,columnType,props){const palette={area:{stroke:'#fb8c00',fill:'rgba(251,140,0,0.12)'},id:{stroke:'#1a73e8',fill:'rgba(26,115,232,0.12)'},name:{stroke:'#2e7d32',fill:'rgba(46,125,50,0.12)'},other:{stroke:'#8e24aa',fill:'rgba(142,36,170,0.12)'}};const key=kind==='area'?'area':columnType==='id'?'id':columnType==='name'?'name':'other';return new fabric.Rect(Object.assign({left:60,top:60,width:160,height:260,fill:palette[key].fill,stroke:palette[key].stroke,strokeWidth:2,transparentCorners:false,cornerColor:palette[key].stroke,cornerStyle:'circle',borderColor:palette[key].stroke,hasRotatingPoint:false,lockRotation:true,kind:kind,columnType:columnType||null},props||{}));}function clearOverlayRects(){canvas.getObjects().filter(o=>o.type==='rect').forEach(o=>canvas.remove(o));canvas.discardActiveObject();canvas.requestRenderAll();}function addAreaRect(){canvas.getObjects().filter(o=>o.type==='rect'&&o.kind==='area').forEach(o=>canvas.remove(o));const rect=makeRect('area',null,{left:40,top:40,width:320,height:420});canvas.add(rect);canvas.setActiveObject(rect);canvas.requestRenderAll();}function addColumnRect(type){const rect=makeRect('column',type,{left:type==='id'?80:type==='name'?270:460,top:80,width:150,height:360});canvas.add(rect);canvas.setActiveObject(rect);canvas.requestRenderAll();}function serializeLayout(){return canvas.getObjects().filter(o=>o.type==='rect').map(o=>({kind:o.kind,columnType:o.columnType||null,left:o.left,top:o.top,width:o.width,height:o.height,scaleX:o.scaleX||1,scaleY:o.scaleY||1}));}function renderProfile(slot){clearOverlayRects();const profile=profiles[slot];if(!profile||!profile.layout)return;profile.layout.forEach(item=>{const rect=makeRect(item.kind,item.columnType,{left:item.left,top:item.top,width:item.width,height:item.height,scaleX:item.scaleX,scaleY:item.scaleY});canvas.add(rect);});canvas.requestRenderAll();}function boxFromRect(rect){return{x:Math.max(0,Math.round(rect.left/imageScale)),y:Math.max(0,Math.round(rect.top/imageScale)),w:Math.max(1,Math.round((rect.width*(rect.scaleX||1))/imageScale)),h:Math.max(1,Math.round((rect.height*(rect.scaleY||1))/imageScale))};}function intersectBoxes(a,b){const x=Math.max(a.x,b.x);const y=Math.max(a.y,b.y);const r=Math.min(a.x+a.w,b.x+b.w);const bt=Math.min(a.y+a.h,b.y+b.h);return r>x&&bt>y?{x:x,y:y,w:r-x,h:bt-y}:null;}function getAreaObject(){return canvas.getObjects().find(o=>o.type==='rect'&&o.kind==='area')||null;}function getColumnObjects(type){return canvas.getObjects().filter(o=>o.type==='rect'&&o.kind==='column'&&o.columnType===type);}function cropBoxToDataUrl(box){const temp=document.createElement('canvas');temp.width=box.w;temp.height=box.h;const ctx=temp.getContext('2d');ctx.drawImage(originalImage,box.x,box.y,box.w,box.h,0,0,box.w,box.h);return temp.toDataURL('image/png');}async function recognizeLines(box,type){const image=cropBoxToDataUrl(box);const lang=type==='id'?'eng':'spa+eng';const result=await Tesseract.recognize(image,lang,{logger:m=>{if(m.status)setStatus('OCR '+type+': '+m.status);}});const lines=(result.data&&result.data.lines&&result.data.lines.length?result.data.lines:[{text:result.data.text||'',bbox:{y0:0,y1:box.h},confidence:result.data.confidence||0}]);return lines.map(line=>{const raw=(line.text||'').replace(/\s+/g,' ').trim();const clean=type==='id'?raw.replace(/[^0-9]/g,''):raw;const yLocal=((line.bbox&&line.bbox.y0)||0)+((((line.bbox&&line.bbox.y1)||box.h)-((line.bbox&&line.bbox.y0)||0))/2);return{raw:raw,clean:clean,conf:Math.round(line.confidence||line.conf||result.data.confidence||0),y:box.y+yLocal};}).filter(item=>type==='id'?item.raw.length>0:item.clean.length>0).sort((a,b)=>a.y-b.y);}function pairByY(idLines,nameLines){const ids=[...idLines].sort((a,b)=>a.y-b.y);const names=[...nameLines].sort((a,b)=>a.y-b.y);const used=new Set();const pairs=[];ids.forEach(id=>{let best=-1;let bestDiff=Infinity;names.forEach((name,idx)=>{if(used.has(idx))return;const diff=Math.abs(name.y-id.y);if(diff<bestDiff){bestDiff=diff;best=idx;}});if(best>=0&&bestDiff<120){used.add(best);pairs.push({id:id,name:names[best]});}else{pairs.push({id:id,name:null});}});names.forEach((name,idx)=>{if(!used.has(idx))pairs.push({id:null,name:name});});return pairs.sort((a,b)=>((a.id&&a.id.y)||(a.name&&a.name.y)||0)-((b.id&&b.id.y)||(b.name&&b.name.y)||0));}function renderResults(){resultsBody.innerHTML='';results.forEach(row=>{const tr=document.createElement('tr');['Identificación','Nombre','Perfil usado','Índice de fila','Texto OCR bruto de identificación','Texto OCR bruto de nombre','Confianza OCR identificación','Confianza OCR nombre','Observaciones'].forEach(key=>{const td=document.createElement('td');td.textContent=row[key]??'';tr.appendChild(td);});resultsBody.appendChild(tr);});}function loadImage(file){if(!file)return;const reader=new FileReader();reader.onload=e=>{currentImageDataUrl=e.target.result;const img=new Image();img.onload=()=>{originalImage=img;imageScale=Math.min(1,1000/img.width,700/img.height);canvas.clear();canvas.setWidth(Math.round(img.width*imageScale));canvas.setHeight(Math.round(img.height*imageScale));fabric.Image.fromURL(currentImageDataUrl,fimg=>{canvas.setBackgroundImage(fimg,canvas.renderAll.bind(canvas),{originX:'left',originY:'top',scaleX:imageScale,scaleY:imageScale});renderProfile(activeSlot);setStatus('Imagen cargada.');});};img.src=currentImageDataUrl;};reader.readAsDataURL(file);}async function runOCR(){if(!originalImage){alert('Primero carga una imagen.');return;}const areaObj=getAreaObject();const areaBox=areaObj?boxFromRect(areaObj):null;const idObjects=getColumnObjects('id');const nameObjects=getColumnObjects('name');if(!idObjects.length&&!nameObjects.length){alert('Debes crear al menos una columna de identificación o nombre.');return;}setStatus('Iniciando OCR...');const idLines=[];for(const obj of idObjects){let box=boxFromRect(obj);if(areaBox){box=intersectBoxes(box,areaBox);}if(box){const lines=await recognizeLines(box,'id');idLines.push(...lines);}}const nameLines=[];for(const obj of nameObjects){let box=boxFromRect(obj);if(areaBox){box=intersectBoxes(box,areaBox);}if(box){const lines=await recognizeLines(box,'name');nameLines.push(...lines);}}const pairs=pairByY(idLines,nameLines);const profileLabel=(profiles[activeSlot]&&profiles[activeSlot].name)?profiles[activeSlot].name:'Bolsillo '+(activeSlot+1);results=pairs.map((pair,index)=>({'Identificación':pair.id?pair.id.clean:'','Nombre':pair.name?pair.name.clean:'','Perfil usado':profileLabel,'Índice de fila':index+1,'Texto OCR bruto de identificación':pair.id?pair.id.raw:'','Texto OCR bruto de nombre':pair.name?pair.name.raw:'','Confianza OCR identificación':pair.id?pair.id.conf:'','Confianza OCR nombre':pair.name?pair.name.conf:'','Observaciones':pair.id&&pair.name?'Emparejado por cercanía vertical':pair.id?'Falta nombre emparejado':'Falta identificación emparejada'}));renderResults();setStatus('OCR finalizado. Registros detectados: '+results.length);}function exportResults(){if(!results.length){alert('No hay resultados para exportar.');return;}const wb=XLSX.utils.book_new();const ws=XLSX.utils.json_to_sheet(results);XLSX.utils.book_append_sheet(wb,ws,'Resultados');XLSX.writeFile(wb,'resultado_ocr_layout.xlsx');setStatus('Archivo XLSX exportado.');}document.getElementById('btnAddArea').addEventListener('click',addAreaRect);document.getElementById('btnAddId').addEventListener('click',()=>addColumnRect('id'));document.getElementById('btnAddName').addEventListener('click',()=>addColumnRect('name'));document.getElementById('btnAddOther').addEventListener('click',()=>addColumnRect('other'));document.getElementById('btnDeleteSelected').addEventListener('click',()=>{const active=canvas.getActiveObject();if(active&&active.type==='rect'){canvas.remove(active);canvas.requestRenderAll();}});document.getElementById('btnClearLayout').addEventListener('click',clearOverlayRects);document.getElementById('btnSaveProfile').addEventListener('click',()=>{profiles[activeSlot]={name:profileName.value.trim()||'Bolsillo '+(activeSlot+1),layout:serializeLayout()};persistProfiles();refreshSlotButtons();setStatus('Perfil guardado en el bolsillo '+(activeSlot+1)+'.');});document.getElementById('btnRunOCR').addEventListener('click',runOCR);document.getElementById('btnExport').addEventListener('click',exportResults);upload.addEventListener('change',e=>loadImage(e.target.files[0]));slotButtons.forEach(btn=>btn.addEventListener('click',()=>{activeSlot=Number(btn.dataset.slot);refreshSlotButtons();if(originalImage)renderProfile(activeSlot);setStatus('Perfil activo cambiado.');}));refreshSlotButtons();canvas.setWidth(1000);canvas.setHeight(700);setStatus('Listo. Carga una imagen para comenzar.');
